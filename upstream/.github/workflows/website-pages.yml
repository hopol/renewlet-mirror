name: Website Pages

# 官网是独立静态站点，只发布 apps/website/dist，不触发 Renewlet 产品镜像或 Worker 部署。
on:
  push:
    branches:
      - main
    paths:
      - "apps/website/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - ".github/workflows/website-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: website-pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1

      - name: Set up Node.js
        uses: actions/setup-node@v7.0.0
        with:
          node-version: 24.19.0

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Configure Pages
        id: pages
        uses: actions/configure-pages@v6.0.0

      - name: Build website
        env:
          RENEWLET_WEBSITE_BASE_URL: ${{ steps.pages.outputs.base_url }}
          RENEWLET_WEBSITE_BASE_PATH: ${{ steps.pages.outputs.base_path }}
          RENEWLET_WEBSITE_REPOSITORY_URL: ${{ github.server_url }}/${{ github.repository }}
          RENEWLET_WEBSITE_ANALYTICS_SCRIPT: ${{ vars.RENEWLET_WEBSITE_ANALYTICS_SCRIPT }}
        run: pnpm --filter @renewlet/website build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5.0.0
        with:
          path: apps/website/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5.0.0
